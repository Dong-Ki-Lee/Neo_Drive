input {
	udp {
		port => 5958
	}
}

filter {
	json {
		source => "message"
	}
	
	kv { 
		source => "message"
		field_split => "()"
		value_split => ","
		trim_key => "'"
		remove_field => [ "message" ]
	}
}

output {
	stdout { codec => rubydebug }
}
